Uses a heap and hash table to create an updating priority queue.
Stores car objects and allows user to retrieve the lowest priced/lowest mileage car of any make and model.
Cars can be updated at any time by the user.
