Class that manipulates arbitrarily large integers.
Integers are stored as byte arrays. Bitwise operations are used to implement addition, subtraction, multiplication, division, modulus, and shifting.
These functions are used to find the XGCD of two integers using the extended euclidean algorithm.
