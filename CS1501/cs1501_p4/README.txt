Given a text file containing a list of nodes and edges, builds an undirected weighted graph.
Analyzes graph to determine the shortest path between nodes, the minimum spanning tree, the connectedness given the failure of two nodes, and other properties.
